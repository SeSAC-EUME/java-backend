<!-- CODEX REVIEW: This copy adds review notes in blockquotes prefixed with [CODEX REVIEW]. Original content follows unchanged. -->

> **[CODEX REVIEW] Summary**
> 1) EumeChatList `updateLastChatTime()` is planned but entity lacks a last-chat timestamp; either add a dedicated column or mutate `updatedAt` explicitly so the method actually persists.
> 2) New ErrorCode/Exception types (EumeChat/UserChat/Admin) must extend the existing `CommonException`/`ErrorCode` framework and be wired into `EumeExceptionHandler`, otherwise they bypass the current response format.
> 3) Auth context is underspecified: `@AuthenticationPrincipal` needs a concrete principal (email vs id). Service methods should accept the authenticated user id and enforce ownership checks before repository calls to prevent cross-user access.
> 4) n8n webhook plan is stubbed; define timeout/retry/error mapping and ensure secrets live in config (with fail-safe responses) to avoid 500s cascading.
> 5) Repo layer/data model expansion (chat/admin) needs DDL alignment: add missing repositories/interfaces and DB constraints (e.g., unique `eume_user_id` for 1:1 chat) before wiring controllers to prevent runtime breaks.
# EUME API 援ы쁽 怨꾪쉷??

> ??臾몄꽌??Claude Code媛 李멸퀬?섏뿬 援ы쁽?????덈룄濡??묒꽦???곸꽭 援ы쁽 怨꾪쉷?쒖엯?덈떎.

---

## 1. User - EumeChat 湲곕뒫

AI 梨꾪똿(?댁쓬?댁?????? 愿??API?낅땲??

### 1.1 EumeChatList 議댁옱 ?щ? 議고쉶

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/eume/chat/me` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | 濡쒓렇?명븳 ?ъ슜?먯쓽 EumeChatList ?곸꽭 ?뺣낫 議고쉶 |

> [CODEX REVIEW] Clarify principal: decide whether @AuthenticationPrincipal provides user id or email; pass that into services and enforce ownership before any repository query (avoid cross-user access).
#### Request
- Header: `Authorization: Bearer {token}` (荑좏궎)
- Body: ?놁쓬

#### Response
```java
// EumeChatListDetailResponse.java
public record EumeChatListDetailResponse(
    Long id,                      // EumeChatList ID (?놁쑝硫?null)
    String chatStatus,            // ACTIVE / DEACTIVATED (?놁쑝硫?null)
    LocalDateTime createdAt,      // ?앹꽦?쇱떆 (?놁쑝硫?null)
    LocalDateTime updatedAt       // ?섏젙?쇱떆 (?놁쑝硫?null)
) {
    public static EumeChatListDetailResponse from(EumeChatList chatList) {
        if (chatList == null) {
            return new EumeChatListDetailResponse(null, null, null, null);
        }
        return new EumeChatListDetailResponse(
            chatList.getId(),
            chatList.getChatStatus().name(),
            chatList.getCreatedAt(),
            chatList.getUpdatedAt()
        );
    }
}
```

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `EumeChatController.java` | 湲곗〈 ?뚯씪??硫붿꽌??異붽? |
| Service | `EumeChatSearchService.java` | ?좉퇋 ?앹꽦 - 議고쉶 ?꾩슜 |
| Repository | `EumeChatListRepository.java` | ?좉퇋 ?앹꽦 |
| DTO | `EumeChatListDetailResponse.java` | ?좉퇋 ?앹꽦 |

#### ?쒕퉬??硫붿꽌??
```java
// EumeChatSearchService.java
@RequiredArgsConstructor
@Transactional(readOnly = true)
@Service
public class EumeChatSearchService {
    private final EumeChatListRepository eumeChatListRepository;
    private final EumeChatContentRepository eumeChatContentRepository;

    public Optional<EumeChatList> findByEumeUserIdOrOptional(Long eumeUserId) {
        return eumeChatListRepository.findByEumeUserId(eumeUserId);
    }

    public EumeChatList findByEumeUserId(Long eumeUserId) {
        return eumeChatListRepository.findByEumeUserId(eumeUserId)
            .orElseThrow(() -> new EumeChatException(EumeChatErrorCode.CHAT_LIST_NOT_FOUND));
    }

    public EumeChatList findById(Long chatListId) {
        return eumeChatListRepository.findById(chatListId)
            .orElseThrow(() -> new EumeChatException(EumeChatErrorCode.CHAT_LIST_NOT_FOUND));
    }
}
```

#### Repository 硫붿꽌??
```java
// EumeChatListRepository.java
public interface EumeChatListRepository extends JpaRepository<EumeChatList, Long> {
    Optional<EumeChatList> findByEumeUserId(Long eumeUserId);
    boolean existsByEumeUserId(Long eumeUserId);
}
```

---

### 1.2 EumeChatList ?앹꽦

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/eume/chat` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | 濡쒓렇?명븳 ?ъ슜?먯쓽 EumeChatList ?앹꽦 (1??1媛쒕쭔 媛?? |

#### Request
- Header: `Authorization: Bearer {token}` (荑좏궎)
- Body: ?놁쓬 (?ъ슜???뺣낫??JWT?먯꽌 異붿텧)

#### Response
```java
// EumeChatListCreateResponse.java
public record EumeChatListCreateResponse(
    Long id,
    String chatStatus,
    LocalDateTime createdAt
) {
    public static EumeChatListCreateResponse from(EumeChatList chatList) {
        return new EumeChatListCreateResponse(
            chatList.getId(),
            chatList.getChatStatus().name(),
            chatList.getCreatedAt()
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| EC_0001 | 409 CONFLICT | ?대? EumeChatList媛 議댁옱?섎뒗 寃쎌슦 |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `EumeChatController.java` | 湲곗〈 ?뚯씪??硫붿꽌??異붽? |
| Service | `EumeChatRegisterService.java` | ?좉퇋 ?앹꽦 - ?깅줉 ?꾩슜 |
| DTO | `EumeChatListCreateResponse.java` | ?좉퇋 ?앹꽦 |
| ErrorCode | `EumeChatErrorCode.java` | ?좉퇋 ?앹꽦 |
| Exception | `EumeChatException.java` | ?좉퇋 ?앹꽦 |

#### Entity ?섏젙 (EumeChatList.java)
```java
// ?뺤쟻 ?⑺넗由?硫붿꽌??異붽?
public static EumeChatList ofNewChatList(EumeUser eumeUser) {
    return EumeChatList.builder()
        .eumeUser(eumeUser)
        .chatStatus(ChatStatus.ACTIVE)
        .build();
}
```

#### ?쒕퉬??硫붿꽌??
```java
// EumeChatRegisterService.java
@RequiredArgsConstructor
@Transactional(readOnly = true)
@Service
public class EumeChatRegisterService {
    private final EumeChatListRepository eumeChatListRepository;
    private final EumeChatSearchService eumeChatSearchService;

    @Transactional
    public EumeChatList createChatList(EumeUser eumeUser) {
        // ?대? 議댁옱?섎뒗吏 ?뺤씤
        if (eumeChatListRepository.existsByEumeUserId(eumeUser.getId())) {
            throw new EumeChatException(EumeChatErrorCode.CHAT_LIST_ALREADY_EXISTS);
        }

        EumeChatList chatList = EumeChatList.ofNewChatList(eumeUser);
        return eumeChatListRepository.save(chatList);
    }
}
```

---

### 1.3 EumeChatContent 紐⑸줉 議고쉶 (Pagination)

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/eume/chat/{chatListId}/contents` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | EumeChatList??????댁슜 紐⑸줉 議고쉶 (?섏씠吏?ㅼ씠?? |

#### Request
- Path Variable: `chatListId` (Long)
- Query Parameters:
  - `page` (int, default: 0) - ?섏씠吏 踰덊샇
  - `size` (int, default: 20) - ?섏씠吏 ?ш린

#### Response
```java
// EumeChatContentListResponse.java
public record EumeChatContentListResponse(
    List<EumeChatContentResponse> contents,
    int currentPage,
    int totalPages,
    long totalElements,
    boolean hasNext,
    boolean hasPrevious
) {
    public static EumeChatContentListResponse from(Page<EumeChatContent> page) {
        List<EumeChatContentResponse> contents = page.getContent().stream()
            .map(EumeChatContentResponse::from)
            .toList();

        return new EumeChatContentListResponse(
            contents,
            page.getNumber(),
            page.getTotalPages(),
            page.getTotalElements(),
            page.hasNext(),
            page.hasPrevious()
        );
    }
}

// EumeChatContentResponse.java
public record EumeChatContentResponse(
    Long id,
    String messageType,       // USER / EUME
    String messageContent,
    LocalDateTime createdAt
) {
    public static EumeChatContentResponse from(EumeChatContent content) {
        return new EumeChatContentResponse(
            content.getId(),
            content.getMessageType(),
            content.getMessageContent(),
            content.getCreatedAt()
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| EC_0002 | 404 NOT_FOUND | EumeChatList媛 議댁옱?섏? ?딅뒗 寃쎌슦 |
| EC_0003 | 400 BAD_REQUEST | ?섏씠吏 踰덊샇媛 ?뚯닔??寃쎌슦 |
| EC_0004 | 400 BAD_REQUEST | ?섏씠吏 ?ш린媛 ?뚯닔??寃쎌슦 |
| EC_0005 | 403 FORBIDDEN | 蹂몄씤??梨꾪똿 紐⑸줉???꾨땶 寃쎌슦 |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `EumeChatController.java` | 湲곗〈 ?뚯씪??硫붿꽌??異붽? |
| Service | `EumeChatSearchService.java` | 硫붿꽌??異붽? |
| Repository | `EumeChatContentRepository.java` | ?좉퇋 ?앹꽦 |
| DTO | `EumeChatContentListResponse.java` | ?좉퇋 ?앹꽦 |
| DTO | `EumeChatContentResponse.java` | ?좉퇋 ?앹꽦 |

#### Repository 硫붿꽌??
```java
// EumeChatContentRepository.java
public interface EumeChatContentRepository extends JpaRepository<EumeChatContent, Long> {
    Page<EumeChatContent> findByEumeChatListIdOrderByCreatedAtDesc(
        Long eumeChatListId,
        Pageable pageable
    );
}
```

#### ?쒕퉬??硫붿꽌??
```java
// EumeChatSearchService.java??異붽?
public Page<EumeChatContent> findContentsByEumeChatListId(Long chatListId, int page, int size) {
    // ?좏슚??寃利?
    if (page < 0) {
        throw new EumeChatException(EumeChatErrorCode.INVALID_PAGE_NUMBER);
    }
    if (size < 0) {
        throw new EumeChatException(EumeChatErrorCode.INVALID_PAGE_SIZE);
    }

    Pageable pageable = PageRequest.of(page, size);
    return eumeChatContentRepository.findByEumeChatListIdOrderByCreatedAtDesc(chatListId, pageable);
}
```

---

### 1.4 EumeChatContent ?앹꽦 (n8n ?뱁썒 ?곕룞)

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/eume/chat/{chatListId}/contents` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | ?ъ슜??硫붿떆吏 ?????n8n ?뱁썒 ?몄텧?섏뿬 AI ?묐떟 ?앹꽦 |

#### Request
```java
// EumeChatContentCreateRequest.java
public record EumeChatContentCreateRequest(
    @NotBlank(message = "硫붿떆吏 ?댁슜? ?꾩닔?낅땲??)
    String messageContent
) {}
```

#### Response
```java
// EumeChatContentCreateResponse.java
public record EumeChatContentCreateResponse(
    EumeChatContentResponse userMessage,   // ?ъ슜?먭? 蹂대궦 硫붿떆吏
    EumeChatContentResponse eumeMessage    // AI ?묐떟 硫붿떆吏
) {
    public static EumeChatContentCreateResponse from(
        EumeChatContent userContent,
        EumeChatContent eumeContent
    ) {
        return new EumeChatContentCreateResponse(
            EumeChatContentResponse.from(userContent),
            EumeChatContentResponse.from(eumeContent)
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| EC_0002 | 404 NOT_FOUND | EumeChatList媛 議댁옱?섏? ?딅뒗 寃쎌슦 |
| EC_0005 | 403 FORBIDDEN | 蹂몄씤??梨꾪똿 紐⑸줉???꾨땶 寃쎌슦 |
| EC_0006 | 400 BAD_REQUEST | 硫붿떆吏 ?댁슜??鍮꾩뼱?덈뒗 寃쎌슦 |
| EC_0007 | 500 INTERNAL_SERVER_ERROR | n8n ?뱁썒 ?몄텧 ?ㅽ뙣 |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `EumeChatController.java` | 湲곗〈 ?뚯씪??硫붿꽌??異붽? |
| Service | `EumeChatService.java` | ?좉퇋 ?앹꽦 - 鍮꾩쫰?덉뒪 濡쒖쭅 |
| Service | `EumeChatRegisterService.java` | 硫붿꽌??異붽? |
| DTO | `EumeChatContentCreateRequest.java` | ?좉퇋 ?앹꽦 |
| DTO | `EumeChatContentCreateResponse.java` | ?좉퇋 ?앹꽦 |

#### Entity ?섏젙 (EumeChatContent.java)
```java
// ?뺤쟻 ?⑺넗由?硫붿꽌??異붽?
public static EumeChatContent ofUserMessage(
    EumeChatList chatList,
    EumeUser user,
    String content
) {
    return EumeChatContent.builder()
        .eumeChatList(chatList)
        .eumeUser(user)
        .messageType("USER")
        .messageContent(content)
        .build();
}

public static EumeChatContent ofEumeMessage(
    EumeChatList chatList,
    EumeUser user,
    String content
) {
    return EumeChatContent.builder()
        .eumeChatList(chatList)
        .eumeUser(user)
        .messageType("EUME")
        .messageContent(content)
        .build();
}
```

#### Entity ?섏젙 (EumeChatList.java)
> [CODEX REVIEW] last chat 시간 컬럼이 없어 updateLastChatTime()이 DB에 반영되지 않습니다. lastChatAt 컬럼을 추가하거나 updatedAt을 명시적으로 set/save 해 주세요.
```java
// 留덉?留?????쒓컙 ?낅뜲?댄듃 硫붿꽌??異붽?
public void updateLastChatTime() {
    // updatedAt? @UpdateTimestamp濡??먮룞 媛깆떊??
}
```

#### ?쒕퉬??硫붿꽌??
```java
// EumeChatService.java
@Slf4j
@RequiredArgsConstructor
@Transactional(readOnly = true)
@Service
public class EumeChatService {
    private final EumeChatSearchService eumeChatSearchService;
    private final EumeChatRegisterService eumeChatRegisterService;
    private final EumeUserSearchService eumeUserSearchService;
    private final WebClient webClient; // n8n ?뱁썒 ?몄텧??

    @Value("${n8n.webhook.eume-chat.url}")
    private String n8nWebhookUrl;

    @Transactional
    public EumeChatContentCreateResponse sendMessage(
        String userEmail,
        Long chatListId,
        EumeChatContentCreateRequest request
    ) {
        // 1. ?ъ슜??議고쉶
        EumeUser user = eumeUserSearchService.findByEmail(userEmail);

        // 2. 梨꾪똿 紐⑸줉 議고쉶 諛?沅뚰븳 ?뺤씤
        EumeChatList chatList = eumeChatSearchService.findById(chatListId);
        validateChatListOwnership(chatList, user);

        // 3. ?ъ슜??硫붿떆吏 ???
        EumeChatContent userContent = eumeChatRegisterService.saveUserMessage(
            chatList, user, request.messageContent()
        );

        // 4. n8n ?뱁썒 ?몄텧?섏뿬 AI ?묐떟 諛쏄린
        String eumeResponse = callN8nWebhook(request.messageContent());

        // 5. AI ?묐떟 ???
        EumeChatContent eumeContent = eumeChatRegisterService.saveEumeMessage(
            chatList, user, eumeResponse
        );

        // 6. 梨꾪똿 紐⑸줉 ?낅뜲?댄듃 ?쒓컙 媛깆떊
        chatList.updateLastChatTime();

        return EumeChatContentCreateResponse.from(userContent, eumeContent);
    }

    private void validateChatListOwnership(EumeChatList chatList, EumeUser user) {
        if (!chatList.getEumeUser().getId().equals(user.getId())) {
            throw new EumeChatException(EumeChatErrorCode.FORBIDDEN_ACCESS);
        }
    }

    private String callN8nWebhook(String message) {
        // WebClient瑜??ъ슜??n8n ?뱁썒 ?몄텧
        // ?ㅽ뙣 ??EumeChatException 諛쒖깮
    }
}
```

---

### 1.5 EumeChat
> [CODEX REVIEW] Wire new ErrorCode/Exception types into the existing CommonException/ErrorCode pattern and EumeExceptionHandler to keep responses consistent (avoid falling back to default 500s).`r`n ?먮윭 肄붾뱶

```java
// EumeChatErrorCode.java
public enum EumeChatErrorCode implements ErrorCode {
    CHAT_LIST_ALREADY_EXISTS(HttpStatus.CONFLICT, "EC_0001", "?대? 梨꾪똿 紐⑸줉??議댁옱?⑸땲??),
    CHAT_LIST_NOT_FOUND(HttpStatus.NOT_FOUND, "EC_0002", "梨꾪똿 紐⑸줉??李얠쓣 ???놁뒿?덈떎"),
    INVALID_PAGE_NUMBER(HttpStatus.BAD_REQUEST, "EC_0003", "?섏씠吏 踰덊샇??0 ?댁긽?댁뼱???⑸땲??),
    INVALID_PAGE_SIZE(HttpStatus.BAD_REQUEST, "EC_0004", "?섏씠吏 ?ш린??1 ?댁긽?댁뼱???⑸땲??),
    FORBIDDEN_ACCESS(HttpStatus.FORBIDDEN, "EC_0005", "?묎렐 沅뚰븳???놁뒿?덈떎"),
    EMPTY_MESSAGE_CONTENT(HttpStatus.BAD_REQUEST, "EC_0006", "硫붿떆吏 ?댁슜??鍮꾩뼱?덉뒿?덈떎"),
    N8N_WEBHOOK_ERROR(HttpStatus.INTERNAL_SERVER_ERROR, "EC_0007", "AI ?묐떟 ?앹꽦???ㅽ뙣?덉뒿?덈떎");

    // ... 湲곗〈 ErrorCode ?⑦꽩怨??숈씪?섍쾶 援ы쁽
}
```

---

## 2. User - UserChat 湲곕뒫

?ъ슜??媛?梨꾪똿 愿??API?낅땲??

### 2.1 UserChatList 紐⑸줉 議고쉶

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/user-chats` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | 濡쒓렇?명븳 ?ъ슜?먯쓽 梨꾪똿諛?紐⑸줉 議고쉶 |

#### Request
- Header: `Authorization: Bearer {token}` (荑좏궎)
- Query Parameters:
  - `page` (int, default: 0) - ?섏씠吏 踰덊샇
  - `size` (int, default: 20) - ?섏씠吏 ?ш린

#### Response
```java
// UserChatListListResponse.java
public record UserChatListListResponse(
    List<UserChatListResponse> chatRooms,
    int currentPage,
    int totalPages,
    long totalElements,
    boolean hasNext,
    boolean hasPrevious
) {
    public static UserChatListListResponse from(Page<UserChatList> page) {
        List<UserChatListResponse> chatRooms = page.getContent().stream()
            .map(UserChatListResponse::from)
            .toList();

        return new UserChatListListResponse(
            chatRooms,
            page.getNumber(),
            page.getTotalPages(),
            page.getTotalElements(),
            page.hasNext(),
            page.hasPrevious()
        );
    }
}

// UserChatListResponse.java
public record UserChatListResponse(
    Long id,
    String roomTitle,
    String roomStatus,
    LocalDateTime createdAt,
    LocalDateTime updatedAt
) {
    public static UserChatListResponse from(UserChatList chatList) {
        return new UserChatListResponse(
            chatList.getId(),
            chatList.getRoomTitle(),
            chatList.getRoomStatus(),
            chatList.getCreatedAt(),
            chatList.getUpdatedAt()
        );
    }
}
```

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `UserChatController.java` | ?좉퇋 ?앹꽦 |
| Service | `UserChatSearchService.java` | ?좉퇋 ?앹꽦 |
| Repository | `UserChatListRepository.java` | ?좉퇋 ?앹꽦 |
| DTO | `UserChatListListResponse.java` | ?좉퇋 ?앹꽦 |
| DTO | `UserChatListResponse.java` | ?좉퇋 ?앹꽦 |

#### Repository 硫붿꽌??
```java
// UserChatListRepository.java
public interface UserChatListRepository extends JpaRepository<UserChatList, Long> {
    Page<UserChatList> findByEumeUserIdOrderByUpdatedAtDesc(Long eumeUserId, Pageable pageable);
}
```

---

### 2.2 UserChatList ?앹꽦

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/user-chats` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | ?덈줈??梨꾪똿諛??앹꽦 |

#### Request
```java
// UserChatListCreateRequest.java
public record UserChatListCreateRequest(
    @NotBlank(message = "梨꾪똿諛??쒕ぉ? ?꾩닔?낅땲??)
    @Size(max = 100, message = "梨꾪똿諛??쒕ぉ? 100???대궡?ъ빞 ?⑸땲??)
    String roomTitle
) {}
```

#### Response
```java
// UserChatListCreateResponse.java
public record UserChatListCreateResponse(
    Long id,
    String roomTitle,
    String roomStatus,
    LocalDateTime createdAt
) {
    public static UserChatListCreateResponse from(UserChatList chatList) {
        return new UserChatListCreateResponse(
            chatList.getId(),
            chatList.getRoomTitle(),
            chatList.getRoomStatus(),
            chatList.getCreatedAt()
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| UC_0001 | 400 BAD_REQUEST | 梨꾪똿諛??쒕ぉ??鍮꾩뼱?덈뒗 寃쎌슦 |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `UserChatController.java` | 硫붿꽌??異붽? |
| Service | `UserChatRegisterService.java` | ?좉퇋 ?앹꽦 |
| DTO | `UserChatListCreateRequest.java` | ?좉퇋 ?앹꽦 |
| DTO | `UserChatListCreateResponse.java` | ?좉퇋 ?앹꽦 |
| ErrorCode | `UserChatErrorCode.java` | ?좉퇋 ?앹꽦 |
| Exception | `UserChatException.java` | ?좉퇋 ?앹꽦 |

#### Entity ?섏젙 (UserChatList.java)
```java
// ?뺤쟻 ?⑺넗由?硫붿꽌??異붽?
public static UserChatList ofNewChatRoom(EumeUser eumeUser, String roomTitle) {
    return UserChatList.builder()
        .eumeUser(eumeUser)
        .roomTitle(roomTitle)
        .roomStatus("ACTIVE")
        .build();
}
```

---

### 2.3 UserChatContent 紐⑸줉 議고쉶 (Pagination)

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/user-chats/{chatListId}/contents` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | 梨꾪똿諛⑹쓽 硫붿떆吏 紐⑸줉 議고쉶 (?섏씠吏?ㅼ씠?? |

#### Request
- Path Variable: `chatListId` (Long)
- Query Parameters:
  - `page` (int, default: 0) - ?섏씠吏 踰덊샇
  - `size` (int, default: 20) - ?섏씠吏 ?ш린

#### Response
```java
// UserChatContentListResponse.java
public record UserChatContentListResponse(
    List<UserChatContentResponse> contents,
    int currentPage,
    int totalPages,
    long totalElements,
    boolean hasNext,
    boolean hasPrevious
) {
    public static UserChatContentListResponse from(Page<UserChatContent> page) {
        List<UserChatContentResponse> contents = page.getContent().stream()
            .map(UserChatContentResponse::from)
            .toList();

        return new UserChatContentListResponse(
            contents,
            page.getNumber(),
            page.getTotalPages(),
            page.getTotalElements(),
            page.hasNext(),
            page.hasPrevious()
        );
    }
}

// UserChatContentResponse.java
public record UserChatContentResponse(
    Long id,
    Long senderId,           // 諛쒖떊??ID
    String senderName,       // 諛쒖떊???대쫫
    String messageType,
    String messageContent,
    LocalDateTime createdAt
) {
    public static UserChatContentResponse from(UserChatContent content) {
        return new UserChatContentResponse(
            content.getId(),
            content.getEumeUser().getId(),
            content.getEumeUser().getUserName(),
            content.getMessageType(),
            content.getMessageContent(),
            content.getCreatedAt()
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| UC_0002 | 404 NOT_FOUND | 梨꾪똿諛⑹쓣 李얠쓣 ???녿뒗 寃쎌슦 |
| UC_0003 | 403 FORBIDDEN | 梨꾪똿諛??묎렐 沅뚰븳???녿뒗 寃쎌슦 |
| UC_0004 | 400 BAD_REQUEST | ?섏씠吏 踰덊샇媛 ?뚯닔??寃쎌슦 |
| UC_0005 | 400 BAD_REQUEST | ?섏씠吏 ?ш린媛 ?뚯닔??寃쎌슦 |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `UserChatController.java` | 硫붿꽌??異붽? |
| Service | `UserChatSearchService.java` | 硫붿꽌??異붽? |
| Repository | `UserChatContentRepository.java` | ?좉퇋 ?앹꽦 |
| DTO | `UserChatContentListResponse.java` | ?좉퇋 ?앹꽦 |
| DTO | `UserChatContentResponse.java` | ?좉퇋 ?앹꽦 |

#### Repository 硫붿꽌??
```java
// UserChatContentRepository.java
public interface UserChatContentRepository extends JpaRepository<UserChatContent, Long> {
    Page<UserChatContent> findByUserChatListIdOrderByCreatedAtDesc(
        Long userChatListId,
        Pageable pageable
    );
}
```

---

### 2.4 UserChatContent ?앹꽦 (硫붿떆吏 諛쒖넚)

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/user-chats/{chatListId}/contents` |
| **?몄쬆** | JWT ?꾩슂 (@AuthenticationPrincipal) |
| **?ㅻ챸** | 梨꾪똿諛⑹뿉 硫붿떆吏 諛쒖넚 |

#### Request
```java
// UserChatContentCreateRequest.java
public record UserChatContentCreateRequest(
    @NotBlank(message = "硫붿떆吏 ?댁슜? ?꾩닔?낅땲??)
    String messageContent,

    @NotBlank(message = "硫붿떆吏 ??낆? ?꾩닔?낅땲??)
    String messageType   // TEXT, IMAGE, FILE ??
) {}
```

#### Response
```java
// UserChatContentCreateResponse.java
public record UserChatContentCreateResponse(
    Long id,
    Long senderId,
    String senderName,
    String messageType,
    String messageContent,
    LocalDateTime createdAt
) {
    public static UserChatContentCreateResponse from(UserChatContent content) {
        return new UserChatContentCreateResponse(
            content.getId(),
            content.getEumeUser().getId(),
            content.getEumeUser().getUserName(),
            content.getMessageType(),
            content.getMessageContent(),
            content.getCreatedAt()
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| UC_0002 | 404 NOT_FOUND | 梨꾪똿諛⑹쓣 李얠쓣 ???녿뒗 寃쎌슦 |
| UC_0003 | 403 FORBIDDEN | 梨꾪똿諛??묎렐 沅뚰븳???녿뒗 寃쎌슦 |
| UC_0006 | 400 BAD_REQUEST | 硫붿떆吏 ?댁슜??鍮꾩뼱?덈뒗 寃쎌슦 |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `UserChatController.java` | 硫붿꽌??異붽? |
| Service | `UserChatService.java` | ?좉퇋 ?앹꽦 |
| Service | `UserChatRegisterService.java` | 硫붿꽌??異붽? |
| DTO | `UserChatContentCreateRequest.java` | ?좉퇋 ?앹꽦 |
| DTO | `UserChatContentCreateResponse.java` | ?좉퇋 ?앹꽦 |

#### Entity ?섏젙 (UserChatContent.java)
```java
// ?뺤쟻 ?⑺넗由?硫붿꽌??異붽?
public static UserChatContent ofNewMessage(
    UserChatList chatList,
    EumeUser sender,
    String messageType,
    String messageContent
) {
    return UserChatContent.builder()
        .userChatList(chatList)
        .eumeUser(sender)
        .messageType(messageType)
        .messageContent(messageContent)
        .build();
}
```

---

### 2.5 UserChat ?먮윭 肄붾뱶

```java
// UserChatErrorCode.java
public enum UserChatErrorCode implements ErrorCode {
    EMPTY_ROOM_TITLE(HttpStatus.BAD_REQUEST, "UC_0001", "梨꾪똿諛??쒕ぉ??鍮꾩뼱?덉뒿?덈떎"),
    CHAT_ROOM_NOT_FOUND(HttpStatus.NOT_FOUND, "UC_0002", "梨꾪똿諛⑹쓣 李얠쓣 ???놁뒿?덈떎"),
    FORBIDDEN_ACCESS(HttpStatus.FORBIDDEN, "UC_0003", "梨꾪똿諛??묎렐 沅뚰븳???놁뒿?덈떎"),
    INVALID_PAGE_NUMBER(HttpStatus.BAD_REQUEST, "UC_0004", "?섏씠吏 踰덊샇??0 ?댁긽?댁뼱???⑸땲??),
    INVALID_PAGE_SIZE(HttpStatus.BAD_REQUEST, "UC_0005", "?섏씠吏 ?ш린??1 ?댁긽?댁뼱???⑸땲??),
    EMPTY_MESSAGE_CONTENT(HttpStatus.BAD_REQUEST, "UC_0006", "硫붿떆吏 ?댁슜??鍮꾩뼱?덉뒿?덈떎");

    // ... 湲곗〈 ErrorCode ?⑦꽩怨??숈씪?섍쾶 援ы쁽
}
```

---

## 3. Admin 湲곕뒫

愿由ъ옄 ?꾩슜 API?낅땲??

### 3.1 Admin 濡쒓렇??

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/admin/login` |
| **?몄쬆** | ?놁쓬 |
| **?ㅻ챸** | 愿由ъ옄 濡쒓렇??(ID/PW ?몄쬆) |

#### Request
```java
// AdminLoginRequest.java
public record AdminLoginRequest(
    @NotBlank(message = "濡쒓렇??ID???꾩닔?낅땲??)
    String adminLoginId,

    @NotBlank(message = "鍮꾨?踰덊샇???꾩닔?낅땲??)
    String adminPw
) {}
```

#### Response
```java
// AdminLoginResponse.java
public record AdminLoginResponse(
    Long id,
    String adminName,
    String adminEmail,
    LocalDateTime lastLoginDate
) {
    public static AdminLoginResponse from(EumeAdmin admin) {
        return new AdminLoginResponse(
            admin.getId(),
            admin.getAdminName(),
            admin.getAdminEmail(),
            admin.getLastLoginDate()
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| AD_0001 | 401 UNAUTHORIZED | 濡쒓렇??ID媛 議댁옱?섏? ?딅뒗 寃쎌슦 |
| AD_0002 | 401 UNAUTHORIZED | 鍮꾨?踰덊샇媛 ?쇱튂?섏? ?딅뒗 寃쎌슦 |
| AD_0003 | 403 FORBIDDEN | 鍮꾪솢?깊솕??愿由ъ옄 怨꾩젙??寃쎌슦 |
| AD_0004 | 423 LOCKED | 濡쒓렇???ㅽ뙣 ?잛닔 珥덇낵 (5???댁긽) |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `AdminController.java` | ?좉퇋 ?앹꽦 |
| Service | `AdminService.java` | ?좉퇋 ?앹꽦 |
| Service | `AdminSearchService.java` | ?좉퇋 ?앹꽦 |
| Repository | `EumeAdminRepository.java` | ?좉퇋 ?앹꽦 |
| DTO | `AdminLoginRequest.java` | ?좉퇋 ?앹꽦 |
| DTO | `AdminLoginResponse.java` | ?좉퇋 ?앹꽦 |
| ErrorCode | `AdminErrorCode.java` | ?좉퇋 ?앹꽦 |
| Exception | `AdminException.java` | ?좉퇋 ?앹꽦 |

#### Entity ?섏젙 (EumeAdmin.java)
```java
// 濡쒓렇???깃났 ???낅뜲?댄듃 硫붿꽌??
public void loginSuccess() {
    this.lastLoginDate = LocalDateTime.now();
    this.loginFailCount = 0;
}

// 濡쒓렇???ㅽ뙣 ???낅뜲?댄듃 硫붿꽌??
public void loginFail() {
    this.loginFailCount++;
}

// 怨꾩젙 ?좉툑 ?щ? ?뺤씤
public boolean isLocked() {
    return this.loginFailCount >= 5;
}

// ?쒖꽦???щ? ?뺤씤
public boolean isActive() {
    return "ACTIVE".equals(this.adminStatus);
}
```

#### ?쒕퉬??硫붿꽌??
```java
// AdminService.java
@Slf4j
@RequiredArgsConstructor
@Transactional(readOnly = true)
@Service
public class AdminService {
    private final AdminSearchService adminSearchService;
    private final EumeAdminRepository adminRepository;
    private final PasswordEncoder passwordEncoder;
    private final JwtUtil jwtUtil;

    @Transactional
    public AdminLoginResponse login(AdminLoginRequest request, HttpServletResponse response) {
        // 1. 愿由ъ옄 議고쉶
        EumeAdmin admin = adminSearchService.findByAdminLoginId(request.adminLoginId());

        // 2. 怨꾩젙 ?곹깭 ?뺤씤
        if (!admin.isActive()) {
            throw new AdminException(AdminErrorCode.DEACTIVATED_ADMIN);
        }
        if (admin.isLocked()) {
            throw new AdminException(AdminErrorCode.ACCOUNT_LOCKED);
        }

        // 3. 鍮꾨?踰덊샇 ?뺤씤
        if (!passwordEncoder.matches(request.adminPw(), admin.getAdminPw())) {
            admin.loginFail();
            adminRepository.save(admin);
            throw new AdminException(AdminErrorCode.INVALID_PASSWORD);
        }

        // 4. 濡쒓렇???깃났 泥섎━
        admin.loginSuccess();
        adminRepository.save(admin);

        // 5. JWT ?좏겙 ?앹꽦 諛?荑좏궎 ?ㅼ젙
        // ... (湲곗〈 OAuth2SuccessHandler ?⑦꽩 李멸퀬)

        log.info("Admin login successful: {}", admin.getAdminLoginId());

        return AdminLoginResponse.from(admin);
    }
}
```

---

### 3.2 Admin 濡쒓렇?꾩썐

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/admin/logout` |
| **?몄쬆** | Admin JWT ?꾩슂 |
| **?ㅻ챸** | 愿由ъ옄 濡쒓렇?꾩썐 (JWT 荑좏궎 ?쒓굅) |

#### Request
- Header: `Authorization: Bearer {token}` (荑좏궎)
- Body: ?놁쓬

#### Response
```java
// AdminLogoutResponse.java
public record AdminLogoutResponse(
    String message
) {
    public static AdminLogoutResponse success() {
        return new AdminLogoutResponse("濡쒓렇?꾩썐 ?섏뿀?듬땲??);
    }
}
```

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `AdminController.java` | 硫붿꽌??異붽? |
| Service | `AdminService.java` | 硫붿꽌??異붽? |
| DTO | `AdminLogoutResponse.java` | ?좉퇋 ?앹꽦 |

#### ?쒕퉬??硫붿꽌??
```java
// AdminService.java??異붽?
public void logout(HttpServletResponse response) {
    // JWT 荑좏궎 ?쒓굅 (湲곗〈 EumeUserService.logout 李멸퀬)
    removeCookie(response, JwtRule.ACCESS_PREFIX.getValue());
    removeCookie(response, JwtRule.REFRESH_PREFIX.getValue());
    removeCookie(response, "JSESSIONID");

    log.info("Admin logout successful");
}

private void removeCookie(HttpServletResponse response, String name) {
    Cookie cookie = new Cookie(name, "");
    cookie.setHttpOnly(true);
    cookie.setSecure(true);
    cookie.setPath("/");
    cookie.setMaxAge(0);
    cookie.setAttribute("SameSite", "Strict");
    response.addCookie(cookie);
}
```

---

### 3.3 EumeUser 紐⑸줉 議고쉶

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/admin/users` |
| **?몄쬆** | Admin JWT ?꾩슂 |
| **?ㅻ챸** | ?꾩껜 ?ъ슜??紐⑸줉 議고쉶 (?섏씠吏?ㅼ씠?? |

#### Request
- Query Parameters:
  - `page` (int, default: 0) - ?섏씠吏 踰덊샇
  - `size` (int, default: 20) - ?섏씠吏 ?ш린
  - `status` (String, optional) - ?ъ슜???곹깭 ?꾪꽣 (ACTIVE/DEACTIVATED)
  - `keyword` (String, optional) - 寃?됱뼱 (?대쫫/?대찓??

#### Response
```java
// AdminUserListResponse.java
public record AdminUserListResponse(
    List<AdminUserResponse> users,
    int currentPage,
    int totalPages,
    long totalElements,
    boolean hasNext,
    boolean hasPrevious
) {
    public static AdminUserListResponse from(Page<EumeUser> page) {
        List<AdminUserResponse> users = page.getContent().stream()
            .map(AdminUserResponse::from)
            .toList();

        return new AdminUserListResponse(
            users,
            page.getNumber(),
            page.getTotalPages(),
            page.getTotalElements(),
            page.hasNext(),
            page.hasPrevious()
        );
    }
}

// AdminUserResponse.java
public record AdminUserResponse(
    Long id,
    String email,
    String userName,
    String nickname,
    String userStatus,
    String sigunguName,
    LocalDateTime lastLoginDate,
    LocalDateTime createdAt
) {
    public static AdminUserResponse from(EumeUser user) {
        String sigunguName = user.getSigungu() != null
            ? user.getSigungu().getSigungu()  // Sigungu ?뷀떚?곗쓽 sigungu ?꾨뱶
            : null;

        return new AdminUserResponse(
            user.getId(),
            user.getEmail(),
            user.getUserName(),
            user.getNickname(),
            user.getUserStatus(),
            sigunguName,
            user.getLastLoginDate(),
            user.getCreatedAt()
        );
    }
}
```

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `AdminController.java` | 硫붿꽌??異붽? |
| Service | `AdminSearchService.java` | 硫붿꽌??異붽? |
| Repository | `EumeUserRepository.java` | 硫붿꽌??異붽? (QueryDSL ?먮뒗 Specification) |
| DTO | `AdminUserListResponse.java` | ?좉퇋 ?앹꽦 |
| DTO | `AdminUserResponse.java` | ?좉퇋 ?앹꽦 |

#### Repository 硫붿꽌??
```java
// EumeUserRepository.java??異붽?
Page<EumeUser> findByUserStatusAndUserNameContainingOrEmailContaining(
    String status,
    String nameKeyword,
    String emailKeyword,
    Pageable pageable
);

// ?먮뒗 Specification ?ъ슜
Page<EumeUser> findAll(Specification<EumeUser> spec, Pageable pageable);
```

---

### 3.4 EumeUser ?곸꽭 議고쉶

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/admin/users/{userId}` |
| **?몄쬆** | Admin JWT ?꾩슂 |
| **?ㅻ챸** | ?뱀젙 ?ъ슜???곸꽭 ?뺣낫 議고쉶 |

#### Request
- Path Variable: `userId` (Long)

#### Response
```java
// AdminUserDetailResponse.java
public record AdminUserDetailResponse(
    Long id,
    String email,
    String userName,
    String nickname,
    String profileImage,
    String userStatus,
    String providerId,
    LocalDate birthDate,
    String gender,
    String phone,
    AdminSigunguResponse sigungu,
    String backgroundTheme,
    LocalDateTime lastLoginDate,
    Integer loginFailCount,
    LocalDateTime createdAt,
    LocalDateTime updatedAt
) {
    public static AdminUserDetailResponse from(EumeUser user) {
        AdminSigunguResponse sigungu = user.getSigungu() != null
            ? AdminSigunguResponse.from(user.getSigungu())
            : null;

        return new AdminUserDetailResponse(
            user.getId(),
            user.getEmail(),
            user.getUserName(),
            user.getNickname(),
            user.getProfileImage(),
            user.getUserStatus(),
            user.getProviderId(),
            user.getBirthDate(),
            user.getGender(),
            user.getPhone(),
            sigungu,
            user.getBackgroundTheme().name(),
            user.getLastLoginDate(),
            user.getLoginFailCount(),
            user.getCreatedAt(),
            user.getUpdatedAt()
        );
    }
}

// AdminSigunguResponse.java
public record AdminSigunguResponse(
    Long id,
    String sido,      // ????
    String sigungu    // ??援?援?
) {
    public static AdminSigunguResponse from(Sigungu sigungu) {
        return new AdminSigunguResponse(
            sigungu.getId(),
            sigungu.getSido(),
            sigungu.getSigungu()
        );
    }
}
```

#### ?먮윭 耳?댁뒪
| ?먮윭 肄붾뱶 | HTTP Status | ?곹솴 |
|-----------|-------------|------|
| AD_0005 | 404 NOT_FOUND | ?ъ슜?먮? 李얠쓣 ???녿뒗 寃쎌슦 |

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `AdminController.java` | 硫붿꽌??異붽? |
| Service | `AdminSearchService.java` | 硫붿꽌??異붽? |
| DTO | `AdminUserDetailResponse.java` | ?좉퇋 ?앹꽦 |
| DTO | `AdminSigunguResponse.java` | ?좉퇋 ?앹꽦 |

---

### 3.5 UserEmotion 議고쉶

#### API 紐낆꽭
| ??ぉ | ?댁슜 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/admin/users/{userId}/emotions` |
| **?몄쬆** | Admin JWT ?꾩슂 |
| **?ㅻ챸** | ?뱀젙 ?ъ슜?먯쓽 媛먯젙 遺꾩꽍 湲곕줉 議고쉶 |

#### Request
- Path Variable: `userId` (Long)
- Query Parameters:
  - `page` (int, default: 0) - ?섏씠吏 踰덊샇
  - `size` (int, default: 20) - ?섏씠吏 ?ш린
  - `startDate` (LocalDate, optional) - ?쒖옉??
  - `endDate` (LocalDate, optional) - 醫낅즺??

#### Response
```java
// AdminUserEmotionListResponse.java
public record AdminUserEmotionListResponse(
    List<AdminUserEmotionResponse> emotions,
    int currentPage,
    int totalPages,
    long totalElements,
    boolean hasNext,
    boolean hasPrevious
) {
    public static AdminUserEmotionListResponse from(Page<UserEmotion> page) {
        List<AdminUserEmotionResponse> emotions = page.getContent().stream()
            .map(AdminUserEmotionResponse::from)
            .toList();

        return new AdminUserEmotionListResponse(
            emotions,
            page.getNumber(),
            page.getTotalPages(),
            page.getTotalElements(),
            page.hasNext(),
            page.hasPrevious()
        );
    }
}

// AdminUserEmotionResponse.java
public record AdminUserEmotionResponse(
    Long id,
    Integer depressionScore,
    Integer anxietyScore,
    Integer stressScore,
    Integer emotionScore,
    List<String> keywords,
    LocalDateTime analysisDate,
    String modelVersion
) {
    public static AdminUserEmotionResponse from(UserEmotion emotion) {
        List<String> keywords = parseKeywords(emotion.getKeywords());

        return new AdminUserEmotionResponse(
            emotion.getId(),
            emotion.getDepressionScore(),
            emotion.getAnxietyScore(),
            emotion.getStressScore(),
            emotion.getEmotionScore(),
            keywords,
            emotion.getAnalysisDate(),
            emotion.getModelVersion()
        );
    }

    private static List<String> parseKeywords(String keywordsJson) {
        // JSON ?뚯떛 濡쒖쭅
        // ObjectMapper ?ъ슜?섏뿬 jsonb瑜?List<String>?쇰줈 蹂??
    }
}
```

#### 援ы쁽 ?뚯씪
| ?좏삎 | ?뚯씪紐?| ?ㅻ챸 |
|------|--------|------|
| Controller | `AdminController.java` | 硫붿꽌??異붽? |
| Service | `AdminSearchService.java` | 硫붿꽌??異붽? |
| Repository | `UserEmotionRepository.java` | ?좉퇋 ?앹꽦 |
| DTO | `AdminUserEmotionListResponse.java` | ?좉퇋 ?앹꽦 |
| DTO | `AdminUserEmotionResponse.java` | ?좉퇋 ?앹꽦 |

#### Repository 硫붿꽌??
```java
// UserEmotionRepository.java
public interface UserEmotionRepository extends JpaRepository<UserEmotion, Long> {
    Page<UserEmotion> findByEumeUserIdOrderByAnalysisDateDesc(
        Long eumeUserId,
        Pageable pageable
    );

    Page<UserEmotion> findByEumeUserIdAndAnalysisDateBetweenOrderByAnalysisDateDesc(
        Long eumeUserId,
        LocalDateTime startDate,
        LocalDateTime endDate,
        Pageable pageable
    );
}
```

---

### 3.6 Admin ?먮윭 肄붾뱶

```java
// AdminErrorCode.java
public enum AdminErrorCode implements ErrorCode {
    ADMIN_NOT_FOUND(HttpStatus.UNAUTHORIZED, "AD_0001", "議댁옱?섏? ?딅뒗 愿由ъ옄?낅땲??),
    INVALID_PASSWORD(HttpStatus.UNAUTHORIZED, "AD_0002", "鍮꾨?踰덊샇媛 ?쇱튂?섏? ?딆뒿?덈떎"),
    DEACTIVATED_ADMIN(HttpStatus.FORBIDDEN, "AD_0003", "鍮꾪솢?깊솕??愿由ъ옄 怨꾩젙?낅땲??),
    ACCOUNT_LOCKED(HttpStatus.LOCKED, "AD_0004", "濡쒓렇???ㅽ뙣 ?잛닔 珥덇낵濡?怨꾩젙???좉꼈?듬땲??),
    USER_NOT_FOUND(HttpStatus.NOT_FOUND, "AD_0005", "?ъ슜?먮? 李얠쓣 ???놁뒿?덈떎");

    // ... 湲곗〈 ErrorCode ?⑦꽩怨??숈씪?섍쾶 援ы쁽
}
```

---

## 4. ?뚯씪 ?앹꽦 ?붿빟

### Controller (3媛?
| ?뚯씪紐?| ?ㅻ챸 |
|--------|------|
| `EumeChatController.java` | 湲곗〈 ?뚯씪 ?섏젙 (4媛?API 異붽?) |
| `UserChatController.java` | ?좉퇋 ?앹꽦 (4媛?API) |
| `AdminController.java` | ?좉퇋 ?앹꽦 (5媛?API) |

### Service (8媛?
| ?뚯씪紐?| ?ㅻ챸 |
|--------|------|
| `EumeChatSearchService.java` | ?좉퇋 - EumeChat 議고쉶 |
| `EumeChatRegisterService.java` | ?좉퇋 - EumeChat ?깅줉 |
| `EumeChatService.java` | ?좉퇋 - EumeChat 鍮꾩쫰?덉뒪 濡쒖쭅 |
| `UserChatSearchService.java` | ?좉퇋 - UserChat 議고쉶 |
| `UserChatRegisterService.java` | ?좉퇋 - UserChat ?깅줉 |
| `UserChatService.java` | ?좉퇋 - UserChat 鍮꾩쫰?덉뒪 濡쒖쭅 |
| `AdminSearchService.java` | ?좉퇋 - Admin 議고쉶 |
| `AdminService.java` | ?좉퇋 - Admin 鍮꾩쫰?덉뒪 濡쒖쭅 |

### Repository (6媛?
| ?뚯씪紐?| ?ㅻ챸 |
|--------|------|
| `EumeChatListRepository.java` | ?좉퇋 |
| `EumeChatContentRepository.java` | ?좉퇋 |
| `UserChatListRepository.java` | ?좉퇋 |
| `UserChatContentRepository.java` | ?좉퇋 |
| `UserEmotionRepository.java` | ?좉퇋 |
| `EumeAdminRepository.java` | ?좉퇋 |

### DTO - Request (4媛?
| ?뚯씪紐?| ?ㅻ챸 |
|--------|------|
| `EumeChatContentCreateRequest.java` | EumeChat 硫붿떆吏 ?앹꽦 ?붿껌 |
| `UserChatListCreateRequest.java` | UserChat 梨꾪똿諛??앹꽦 ?붿껌 |
| `UserChatContentCreateRequest.java` | UserChat 硫붿떆吏 ?앹꽦 ?붿껌 |
| `AdminLoginRequest.java` | Admin 濡쒓렇???붿껌 |

### DTO - Response (19媛?
| ?뚯씪紐?| ?ㅻ챸 |
|--------|------|
| `EumeChatListDetailResponse.java` | EumeChat 紐⑸줉 ?곸꽭 |
| `EumeChatListCreateResponse.java` | EumeChat 紐⑸줉 ?앹꽦 |
| `EumeChatContentListResponse.java` | EumeChat ?댁슜 紐⑸줉 |
| `EumeChatContentResponse.java` | EumeChat ?댁슜 ?④굔 |
| `EumeChatContentCreateResponse.java` | EumeChat ?댁슜 ?앹꽦 |
| `UserChatListListResponse.java` | UserChat 梨꾪똿諛?紐⑸줉 |
| `UserChatListResponse.java` | UserChat 梨꾪똿諛??④굔 |
| `UserChatListCreateResponse.java` | UserChat 梨꾪똿諛??앹꽦 |
| `UserChatContentListResponse.java` | UserChat ?댁슜 紐⑸줉 |
| `UserChatContentResponse.java` | UserChat ?댁슜 ?④굔 |
| `UserChatContentCreateResponse.java` | UserChat ?댁슜 ?앹꽦 |
| `AdminLoginResponse.java` | Admin 濡쒓렇???묐떟 |
| `AdminLogoutResponse.java` | Admin 濡쒓렇?꾩썐 ?묐떟 |
| `AdminUserListResponse.java` | Admin ?ъ슜??紐⑸줉 |
| `AdminUserResponse.java` | Admin ?ъ슜???④굔 |
| `AdminUserDetailResponse.java` | Admin ?ъ슜???곸꽭 |
| `AdminSigunguResponse.java` | Admin ?쒓뎔援?|
| `AdminUserEmotionListResponse.java` | Admin 媛먯젙 紐⑸줉 |
| `AdminUserEmotionResponse.java` | Admin 媛먯젙 ?④굔 |

### Exception & ErrorCode (6媛?
| ?뚯씪紐?| ?ㅻ챸 |
|--------|------|
| `EumeChatException.java` | EumeChat ?덉쇅 |
| `EumeChatErrorCode.java` | EumeChat ?먮윭 肄붾뱶 |
| `UserChatException.java` | UserChat ?덉쇅 |
| `UserChatErrorCode.java` | UserChat ?먮윭 肄붾뱶 |
| `AdminException.java` | Admin ?덉쇅 |
| `AdminErrorCode.java` | Admin ?먮윭 肄붾뱶 |

### Entity ?섏젙 (5媛?
| ?뚯씪紐?| ?섏젙 ?댁슜 |
|--------|----------|
| `EumeChatList.java` | ?뺤쟻 ?⑺넗由?硫붿꽌??異붽? |
| `EumeChatContent.java` | ?뺤쟻 ?⑺넗由?硫붿꽌??異붽? |
| `UserChatList.java` | ?뺤쟻 ?⑺넗由?硫붿꽌??異붽? |
| `UserChatContent.java` | ?뺤쟻 ?⑺넗由?硫붿꽌??異붽? |
| `EumeAdmin.java` | 濡쒓렇??愿??硫붿꽌??異붽? |

---

## 5. 援ы쁽 ?쒖꽌 沅뚯옣

### Phase 1: EumeChat 湲곕뒫 (?곗꽑?쒖쐞 ?믪쓬)
1. Repository ?앹꽦 (EumeChatListRepository, EumeChatContentRepository)
2. ErrorCode & Exception ?앹꽦
3. Entity ?섏젙 (?⑺넗由?硫붿꽌??異붽?)
4. SearchService ?앹꽦
5. RegisterService ?앹꽦
6. Service ?앹꽦
7. DTO ?앹꽦
8. Controller 硫붿꽌??異붽?
9. ?뚯뒪??

### Phase 2: UserChat 湲곕뒫
1. Repository ?앹꽦
2. ErrorCode & Exception ?앹꽦
3. Entity ?섏젙
4. Service 怨꾩링 ?앹꽦
5. DTO ?앹꽦
6. Controller ?앹꽦
7. ?뚯뒪??

### Phase 3: Admin 湲곕뒫
1. Repository ?앹꽦
2. ErrorCode & Exception ?앹꽦
3. Entity ?섏젙
4. Service 怨꾩링 ?앹꽦
5. DTO ?앹꽦
6. Controller ?앹꽦
7. Admin ?몄쬆 ?꾪꽣/?ㅼ젙 異붽?
8. ?뚯뒪??

---

## 6. ?ㅼ젙 異붽? ?꾩슂

### application.yaml ?먮뒗 application-local.yaml
```yaml
# n8n ?뱁썒 ?ㅼ젙
n8n:
  webhook:
    eume-chat:
      url: ${N8N_EUME_CHAT_WEBHOOK_URL}
```

### .env
```
N8N_EUME_CHAT_WEBHOOK_URL=https://n8n.example.com/webhook/eume-chat
```

### SecurityConfiguration.java
- Admin API 寃쎈줈??????몄쬆 ?ㅼ젙 異붽?
- `/api/admin/**` 寃쎈줈??Admin JWT 寃利??꾩슂

---

## 7. 異붽? 怨좊젮 ?ы빆

### ?섏씠吏?ㅼ씠???뚮씪誘명꽣 寃利?
Controller?먯꽌 `@Min` ?대끂?뚯씠?섏쑝濡?寃利앺븯??寃껋쓣 沅뚯옣?⑸땲??
```java
@GetMapping("/{chatListId}/contents")
public ResponseEntity<EumeChatContentListResponse> getContents(
    @PathVariable Long chatListId,
    @RequestParam(defaultValue = "0") @Min(0) int page,
    @RequestParam(defaultValue = "20") @Min(1) int size
) { ... }
```

### EumeChat 1??1媛??쒗븳
- `EumeChatList`? `EumeUser`??`@ManyToOne` 愿怨꾩씠吏留? 鍮꾩쫰?덉뒪 濡쒖쭅?먯꽌 1??1媛쒕줈 ?쒗븳
- `EumeChatListRepository.existsByEumeUserId()`濡?以묐났 ?앹꽦 諛⑹?
- ?꾩슂 ??DB??unique ?쒖빟議곌굔 異붽? 怨좊젮: `@Table(uniqueConstraints = @UniqueConstraint(columnNames = "eume_user_id"))`

### UserChat 梨꾪똿諛?李몄뿬??愿由?
- ?꾩옱 `UserChatList`???앹꽦??`eumeUser`)留????
- ?ㅻ???梨꾪똿???꾪빐?쒕뒗 蹂꾨룄??李몄뿬???뚯씠釉??꾩슂?????덉쓬
- ?ν썑 ?뺤옣 ??`UserChatParticipant` ?뷀떚??異붽? 怨좊젮

### Admin JWT 遺꾨━
- User JWT? Admin JWT瑜?遺꾨━?섏뿬 愿由ы븷吏 寃???꾩슂
- 媛숈? JWT瑜??ъ슜??寃쎌슦: ?좏겙??role claim 異붽?
- 遺꾨━??寃쎌슦: Admin ?꾩슜 JWT ?꾪꽣 諛??ㅼ젙 ?꾩슂















