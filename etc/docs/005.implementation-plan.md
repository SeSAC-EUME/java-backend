# EUME 추가 기능 구현 계획서 v5

> 이 문서는 Claude Code가 참고하여 구현할 수 있도록 작성된 상세 구현 계획서입니다.
>
> **v5 신규 기능** (2025-11-27):
> - User 계정 비활성화/탈퇴 기능
> - Admin 이용자 상태 변경 기능
> - Admin 이용자 Excel 내보내기 기능
> - Admin 보고서 요약/다운로드 기능
>
> **참고:** `etc/add-api/000.overall-additional-plan.md` 기반

---

## 1. User 계정 비활성화/탈퇴 기능

사용자가 스스로 계정을 비활성화하거나 탈퇴할 수 있는 기능입니다.

### 1.1 계정 비활성화

#### API 명세
| 항목 | 내용 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/users/me/deactivate` |
| **인증** | JWT 필요 (@AuthenticationPrincipal) |
| **설명** | 로그인한 사용자의 계정을 비활성화 |

#### Swagger 문서화
```java
@PostMapping("/me/deactivate")
@Operation(summary = "계정 비활성화", description = "로그인한 사용자의 계정을 비활성화합니다. 비활성화된 계정은 로그인할 수 없습니다.")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "비활성화 성공"),
    @ApiResponse(responseCode = "401", description = "인증 실패 (JWT 토큰 없음 또는 유효하지 않음)"),
    @ApiResponse(responseCode = "409", description = "이미 비활성화된 계정")
})
public ResponseEntity<UserDeactivateResponse> deactivateAccount(
    @AuthenticationPrincipal UserDetails userDetails,
    HttpServletResponse response
) { ... }
```

#### Request
- Header: `Authorization: Bearer {token}` (쿠키)
- Body: 없음

#### Response
```java
// UserDeactivateResponse.java
public record UserDeactivateResponse(
    String message,
    String userStatus,
    LocalDateTime deactivatedAt
) {
    public static UserDeactivateResponse from(EumeUser user) {
        return new UserDeactivateResponse(
            "계정이 비활성화되었습니다",
            user.getUserStatus(),
            LocalDateTime.now()
        );
    }
}
```

#### 에러 케이스
| 에러 코드 | HTTP Status | 상황 |
|-----------|-------------|------|
| U_0006 | 409 CONFLICT | 이미 비활성화된 계정인 경우 |

#### Entity 수정 (EumeUser.java)
```java
// 계정 비활성화 메서드
public void deactivate() {
    if ("DEACTIVATED".equals(this.userStatus)) {
        throw new EumeUserException(EumeUserErrorCode.ALREADY_DEACTIVATED);
    }
    this.userStatus = "DEACTIVATED";
}

// 계정 활성화 메서드 (Admin용)
public void activate() {
    this.userStatus = "ACTIVE";
}

// 비활성화 여부 확인
public boolean isDeactivated() {
    return "DEACTIVATED".equals(this.userStatus);
}
```

#### 서비스 메서드
```java
// EumeUserService.java에 추가
@Transactional
public EumeUser deactivateUser(String email, HttpServletResponse response) {
    EumeUser user = eumeUserSearchService.findByEmail(email);
    user.deactivate();

    // JWT 쿠키 삭제 (로그아웃 처리)
    deleteCookie(response, JwtRule.ACCESS_PREFIX.getValue());
    deleteCookie(response, JwtRule.REFRESH_PREFIX.getValue());

    return user;
}
```

#### 구현 파일
| 유형 | 파일명 | 설명 |
|------|--------|------|
| Controller | `EumeUserController.java` | 메서드 추가 |
| Service | `EumeUserService.java` | 메서드 추가 |
| Entity | `EumeUser.java` | 메서드 추가 |
| DTO | `UserDeactivateResponse.java` | 신규 생성 |
| ErrorCode | `EumeUserErrorCode.java` | 에러 코드 추가 |

---

### 1.2 계정 탈퇴 (선택)

#### API 명세
| 항목 | 내용 |
|------|------|
| **Method** | POST |
| **Endpoint** | `/api/users/me/withdraw` |
| **인증** | JWT 필요 (@AuthenticationPrincipal) |
| **설명** | 로그인한 사용자의 계정을 탈퇴 처리 (논리 삭제) |

#### Swagger 문서화
```java
@PostMapping("/me/withdraw")
@Operation(summary = "계정 탈퇴", description = "로그인한 사용자의 계정을 탈퇴 처리합니다. 탈퇴 후에는 동일 계정으로 재가입이 불가능합니다.")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "탈퇴 성공"),
    @ApiResponse(responseCode = "401", description = "인증 실패 (JWT 토큰 없음 또는 유효하지 않음)")
})
public ResponseEntity<UserWithdrawResponse> withdrawAccount(
    @AuthenticationPrincipal UserDetails userDetails,
    HttpServletResponse response
) { ... }
```

#### Request
- Header: `Authorization: Bearer {token}` (쿠키)
- Body: 없음

#### Response
```java
// UserWithdrawResponse.java
public record UserWithdrawResponse(
    String message,
    LocalDateTime withdrawnAt
) {
    public static UserWithdrawResponse success() {
        return new UserWithdrawResponse(
            "계정이 탈퇴 처리되었습니다",
            LocalDateTime.now()
        );
    }
}
```

#### Entity 수정 (EumeUser.java)
```java
// 탈퇴 처리 메서드
public void withdraw() {
    this.userStatus = "WITHDRAWN";
    // Google 연동 해제를 위해 providerId 초기화 (선택)
    // this.providerId = null;
}
```

---

### 1.3 User 에러 코드 추가

```java
// EumeUserErrorCode.java에 추가
ALREADY_DEACTIVATED(HttpStatus.CONFLICT, "U_0006", "이미 비활성화된 계정입니다"),
DEACTIVATED_ACCOUNT(HttpStatus.FORBIDDEN, "U_0007", "비활성화된 계정입니다. 관리자에게 문의하세요");
```

---

### 1.4 로그인 차단 로직

비활성화된 계정이 로그인 시도 시 차단하는 로직을 추가합니다.

#### OAuth2SuccessHandler 수정
```java
// OAuth2SuccessHandler.java에서 로그인 성공 처리 전 상태 확인
if (user.isDeactivated()) {
    throw new EumeUserException(EumeUserErrorCode.DEACTIVATED_ACCOUNT);
}
```

---

## 2. Admin 이용자 상태 변경 기능

관리자가 사용자의 상태를 변경할 수 있는 기능입니다.

### 2.1 이용자 상태 변경

#### API 명세
| 항목 | 내용 |
|------|------|
| **Method** | PATCH |
| **Endpoint** | `/api/admin/users/{userId}/status` |
| **인증** | Admin JWT 필요 |
| **설명** | 특정 사용자의 상태를 변경 (ACTIVE/DEACTIVATED) |

#### Swagger 문서화
```java
@PatchMapping("/users/{userId}/status")
@Operation(summary = "이용자 상태 변경", description = "특정 사용자의 상태를 활성/비활성으로 변경합니다.")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "상태 변경 성공"),
    @ApiResponse(responseCode = "400", description = "잘못된 상태 값"),
    @ApiResponse(responseCode = "401", description = "인증 실패"),
    @ApiResponse(responseCode = "404", description = "사용자를 찾을 수 없음")
})
public ResponseEntity<AdminUserStatusUpdateResponse> updateUserStatus(
    @PathVariable Long userId,
    @Valid @RequestBody AdminUserStatusUpdateRequest request
) { ... }
```

#### Request
```java
// AdminUserStatusUpdateRequest.java
public record AdminUserStatusUpdateRequest(
    @NotBlank(message = "상태 값은 필수입니다")
    @Pattern(regexp = "ACTIVE|DEACTIVATED", message = "상태 값은 ACTIVE 또는 DEACTIVATED만 가능합니다")
    String status
) {}
```

#### Response
```java
// AdminUserStatusUpdateResponse.java
public record AdminUserStatusUpdateResponse(
    Long userId,
    String previousStatus,
    String currentStatus,
    LocalDateTime updatedAt
) {
    public static AdminUserStatusUpdateResponse of(Long userId, String prevStatus, String curStatus) {
        return new AdminUserStatusUpdateResponse(
            userId,
            prevStatus,
            curStatus,
            LocalDateTime.now()
        );
    }
}
```

#### 에러 케이스
| 에러 코드 | HTTP Status | 상황 |
|-----------|-------------|------|
| AD_0005 | 404 NOT_FOUND | 사용자를 찾을 수 없는 경우 |
| AD_0010 | 400 BAD_REQUEST | 잘못된 상태 값인 경우 |

#### 서비스 메서드
```java
// AdminService.java에 추가
@Transactional
public AdminUserStatusUpdateResponse updateUserStatus(Long userId, String newStatus) {
    EumeUser user = adminSearchService.findUserById(userId);
    String previousStatus = user.getUserStatus();

    if ("ACTIVE".equals(newStatus)) {
        user.activate();
    } else if ("DEACTIVATED".equals(newStatus)) {
        user.deactivate();
    } else {
        throw new AdminException(AdminErrorCode.INVALID_STATUS);
    }

    return AdminUserStatusUpdateResponse.of(userId, previousStatus, newStatus);
}
```

#### 구현 파일
| 유형 | 파일명 | 설명 |
|------|--------|------|
| Controller | `AdminController.java` | 메서드 추가 |
| Service | `AdminService.java` | 메서드 추가 |
| DTO | `AdminUserStatusUpdateRequest.java` | 신규 생성 |
| DTO | `AdminUserStatusUpdateResponse.java` | 신규 생성 |
| ErrorCode | `AdminErrorCode.java` | 에러 코드 추가 |

---

## 3. Admin 이용자 Excel 내보내기 기능

관리자가 이용자 데이터를 Excel 파일로 내보낼 수 있는 기능입니다.

### 3.1 이용자 Excel 내보내기

#### API 명세
| 항목 | 내용 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/admin/users/export` |
| **인증** | Admin JWT 필요 |
| **설명** | 필터 조건에 맞는 이용자 데이터를 Excel 파일로 다운로드 |

#### Swagger 문서화
```java
@GetMapping("/users/export")
@Operation(summary = "이용자 데이터 Excel 내보내기", description = "필터 조건에 맞는 이용자 데이터를 Excel 파일로 다운로드합니다.")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "다운로드 성공", content = @Content(mediaType = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")),
    @ApiResponse(responseCode = "401", description = "인증 실패"),
    @ApiResponse(responseCode = "500", description = "파일 생성 실패")
})
public ResponseEntity<Resource> exportUsersToExcel(
    @RequestParam(required = false) String status,
    @RequestParam(required = false) String keyword
) { ... }
```

#### Request
- Query Parameters:
  - `status` (String, optional) - 사용자 상태 필터 (ACTIVE/DEACTIVATED)
  - `keyword` (String, optional) - 검색어 (이름/이메일)

#### Response
- Content-Type: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- Content-Disposition: `attachment; filename="users_20251127_120000.xlsx"`
- Body: Excel 파일 바이너리

#### Excel 파일 구조
| 컬럼명 | 설명 |
|--------|------|
| ID | 사용자 ID |
| 이메일 | 이메일 주소 |
| 이름 | 사용자 이름 |
| 닉네임 | 닉네임 |
| 상태 | 계정 상태 (활성/비활성) |
| 지역 | 시군구 |
| 마지막 로그인 | 마지막 로그인 일시 |
| 가입일 | 계정 생성일 |

#### 서비스 메서드
```java
// AdminExportService.java (신규 생성)
@RequiredArgsConstructor
@Service
public class AdminExportService {
    private final AdminSearchService adminSearchService;

    public byte[] exportUsersToExcel(String status, String keyword) {
        List<EumeUser> users = adminSearchService.findAllUsers(status, keyword);

        try (Workbook workbook = new XSSFWorkbook()) {
            Sheet sheet = workbook.createSheet("이용자 목록");

            // 헤더 생성
            Row headerRow = sheet.createRow(0);
            String[] headers = {"ID", "이메일", "이름", "닉네임", "상태", "지역", "마지막 로그인", "가입일"};
            for (int i = 0; i < headers.length; i++) {
                Cell cell = headerRow.createCell(i);
                cell.setCellValue(headers[i]);
            }

            // 데이터 행 생성
            int rowNum = 1;
            for (EumeUser user : users) {
                Row row = sheet.createRow(rowNum++);
                row.createCell(0).setCellValue(user.getId());
                row.createCell(1).setCellValue(user.getEmail());
                row.createCell(2).setCellValue(user.getUserName());
                row.createCell(3).setCellValue(user.getNickname());
                row.createCell(4).setCellValue(user.getUserStatus());
                row.createCell(5).setCellValue(user.getSigungu() != null ? user.getSigungu().getSigungu() : "");
                row.createCell(6).setCellValue(user.getLastLoginDate() != null ? user.getLastLoginDate().toString() : "");
                row.createCell(7).setCellValue(user.getCreatedAt().toString());
            }

            // 컬럼 너비 자동 조정
            for (int i = 0; i < headers.length; i++) {
                sheet.autoSizeColumn(i);
            }

            ByteArrayOutputStream outputStream = new ByteArrayOutputStream();
            workbook.write(outputStream);
            return outputStream.toByteArray();
        } catch (IOException e) {
            throw new AdminException(AdminErrorCode.EXPORT_FAILED);
        }
    }
}
```

#### Controller 메서드
```java
// AdminController.java에 추가
@GetMapping("/users/export")
@Operation(summary = "이용자 Excel 내보내기", description = "이용자 데이터를 Excel 파일로 다운로드합니다.")
public ResponseEntity<Resource> exportUsersToExcel(
    @RequestParam(required = false) String status,
    @RequestParam(required = false) String keyword
) {
    byte[] excelData = adminExportService.exportUsersToExcel(status, keyword);

    String filename = "users_" + LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss")) + ".xlsx";

    return ResponseEntity.ok()
        .contentType(MediaType.parseMediaType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"))
        .header(HttpHeaders.CONTENT_DISPOSITION, "attachment; filename=\"" + filename + "\"")
        .body(new ByteArrayResource(excelData));
}
```

#### 에러 케이스
| 에러 코드 | HTTP Status | 상황 |
|-----------|-------------|------|
| AD_0011 | 500 INTERNAL_SERVER_ERROR | Excel 파일 생성 실패 |

#### 구현 파일
| 유형 | 파일명 | 설명 |
|------|--------|------|
| Controller | `AdminController.java` | 메서드 추가 |
| Service | `AdminExportService.java` | 신규 생성 |
| Service | `AdminSearchService.java` | findAllUsers 메서드 추가 |
| ErrorCode | `AdminErrorCode.java` | 에러 코드 추가 |

#### 의존성 추가 (build.gradle)
```gradle
// Apache POI (Excel)
implementation 'org.apache.poi:poi-ooxml:5.2.5'
```

---

## 4. Admin 보고서 기능

관리자가 보고서 데이터를 조회하고 PDF/Excel로 다운로드할 수 있는 기능입니다.

### 4.1 보고서 요약 데이터 조회

#### API 명세
| 항목 | 내용 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/admin/reports/summary` |
| **인증** | Admin JWT 필요 |
| **설명** | 기간별 보고서 요약 데이터 조회 |

#### Swagger 문서화
```java
@GetMapping("/reports/summary")
@Operation(summary = "보고서 요약 조회", description = "기간별 이용자 활동, AI 대화, 감정 분석 등 요약 데이터를 조회합니다.")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "조회 성공"),
    @ApiResponse(responseCode = "401", description = "인증 실패"),
    @ApiResponse(responseCode = "400", description = "잘못된 날짜 형식")
})
public ResponseEntity<AdminReportSummaryResponse> getReportSummary(
    @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate fromDate,
    @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate toDate
) { ... }
```

#### Request
- Query Parameters:
  - `fromDate` (LocalDate, required) - 시작일 (예: 2025-11-01)
  - `toDate` (LocalDate, required) - 종료일 (예: 2025-11-30)

#### Response
```java
// AdminReportSummaryResponse.java
public record AdminReportSummaryResponse(
    UserActivitySummary userActivity,
    ConversationSummary conversation,
    EmotionSummary emotion,
    List<DailyStatResponse> dailyStats
) {
    public static AdminReportSummaryResponse of(
        UserActivitySummary userActivity,
        ConversationSummary conversation,
        EmotionSummary emotion,
        List<DailyStatResponse> dailyStats
    ) {
        return new AdminReportSummaryResponse(userActivity, conversation, emotion, dailyStats);
    }
}

// UserActivitySummary.java
public record UserActivitySummary(
    long totalUsers,
    long activeUsers,
    long newUsers,
    double avgActivityMinutes
) {}

// ConversationSummary.java
public record ConversationSummary(
    long totalConversations,
    double dailyAvgConversations,
    long totalMessages
) {}

// EmotionSummary.java
public record EmotionSummary(
    long needAttentionUsers,
    double avgEmotionScore,
    Map<String, Long> emotionDistribution
) {}

// DailyStatResponse.java
public record DailyStatResponse(
    LocalDate date,
    long activeUsers,
    long conversations,
    double avgEmotionScore
) {}
```

#### 서비스 메서드
```java
// AdminReportService.java (신규 생성)
@RequiredArgsConstructor
@Transactional(readOnly = true)
@Service
public class AdminReportService {
    private final EumeUserRepository eumeUserRepository;
    private final EumeChatListRepository eumeChatListRepository;
    private final EumeChatContentRepository eumeChatContentRepository;
    private final UserEmotionRepository userEmotionRepository;

    public AdminReportSummaryResponse getReportSummary(LocalDate fromDate, LocalDate toDate) {
        LocalDateTime startDateTime = fromDate.atStartOfDay();
        LocalDateTime endDateTime = toDate.atTime(23, 59, 59);

        // 이용자 활동 요약
        UserActivitySummary userActivity = getUserActivitySummary(startDateTime, endDateTime);

        // 대화 요약
        ConversationSummary conversation = getConversationSummary(startDateTime, endDateTime);

        // 감정 분석 요약
        EmotionSummary emotion = getEmotionSummary(startDateTime, endDateTime);

        // 일별 통계
        List<DailyStatResponse> dailyStats = getDailyStats(fromDate, toDate);

        return AdminReportSummaryResponse.of(userActivity, conversation, emotion, dailyStats);
    }

    private UserActivitySummary getUserActivitySummary(LocalDateTime start, LocalDateTime end) {
        long totalUsers = eumeUserRepository.count();
        long activeUsers = eumeUserRepository.countByUserStatus("ACTIVE");
        long newUsers = eumeUserRepository.countByCreatedAtBetween(start, end);
        // avgActivityMinutes는 대화 기록 기반 계산 (선택)
        return new UserActivitySummary(totalUsers, activeUsers, newUsers, 0.0);
    }

    // ... 기타 메서드
}
```

#### 구현 파일
| 유형 | 파일명 | 설명 |
|------|--------|------|
| Controller | `AdminController.java` | 메서드 추가 |
| Service | `AdminReportService.java` | 신규 생성 |
| DTO | `AdminReportSummaryResponse.java` | 신규 생성 |
| DTO | `UserActivitySummary.java` | 신규 생성 |
| DTO | `ConversationSummary.java` | 신규 생성 |
| DTO | `EmotionSummary.java` | 신규 생성 |
| DTO | `DailyStatResponse.java` | 신규 생성 |
| Repository | `EumeUserRepository.java` | 통계 메서드 추가 |

---

### 4.2 보고서 Excel 다운로드

#### API 명세
| 항목 | 내용 |
|------|------|
| **Method** | GET |
| **Endpoint** | `/api/admin/reports/export` |
| **인증** | Admin JWT 필요 |
| **설명** | 보고서 데이터를 Excel 파일로 다운로드 |

#### Swagger 문서화
```java
@GetMapping("/reports/export")
@Operation(summary = "보고서 Excel 다운로드", description = "보고서 데이터를 Excel 파일로 다운로드합니다.")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "다운로드 성공"),
    @ApiResponse(responseCode = "401", description = "인증 실패"),
    @ApiResponse(responseCode = "400", description = "잘못된 날짜 형식"),
    @ApiResponse(responseCode = "500", description = "파일 생성 실패")
})
public ResponseEntity<Resource> exportReportToExcel(
    @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate fromDate,
    @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate toDate
) { ... }
```

#### Request
- Query Parameters:
  - `fromDate` (LocalDate, required) - 시작일
  - `toDate` (LocalDate, required) - 종료일

#### Response
- Content-Type: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- Content-Disposition: `attachment; filename="report_20251101_20251130.xlsx"`
- Body: Excel 파일 바이너리

#### Excel 파일 구조
**Sheet 1: 요약**
| 항목 | 값 |
|------|---|
| 총 이용자 수 | 1,234 |
| 활성 이용자 수 | 906 |
| 신규 가입자 수 | 45 |
| 총 대화 수 | 156 |
| 평균 감정 점수 | 3.5 |

**Sheet 2: 일별 통계**
| 날짜 | 활성 이용자 | 대화 수 | 평균 감정 점수 |
|------|------------|--------|--------------|
| 2025-11-01 | 15 | 32 | 3.5 |
| 2025-11-02 | 18 | 28 | 3.7 |
| ... | ... | ... | ... |

#### 서비스 메서드
```java
// AdminReportService.java에 추가
public byte[] exportReportToExcel(LocalDate fromDate, LocalDate toDate) {
    AdminReportSummaryResponse summary = getReportSummary(fromDate, toDate);

    try (Workbook workbook = new XSSFWorkbook()) {
        // Sheet 1: 요약
        createSummarySheet(workbook, summary);

        // Sheet 2: 일별 통계
        createDailyStatsSheet(workbook, summary.dailyStats());

        ByteArrayOutputStream outputStream = new ByteArrayOutputStream();
        workbook.write(outputStream);
        return outputStream.toByteArray();
    } catch (IOException e) {
        throw new AdminException(AdminErrorCode.EXPORT_FAILED);
    }
}
```

#### 구현 파일
| 유형 | 파일명 | 설명 |
|------|--------|------|
| Controller | `AdminController.java` | 메서드 추가 |
| Service | `AdminReportService.java` | 메서드 추가 |

---

## 5. Admin 에러 코드 추가

```java
// AdminErrorCode.java에 추가
INVALID_STATUS(HttpStatus.BAD_REQUEST, "AD_0010", "잘못된 상태 값입니다"),
EXPORT_FAILED(HttpStatus.INTERNAL_SERVER_ERROR, "AD_0011", "파일 내보내기에 실패했습니다"),
INVALID_DATE_RANGE(HttpStatus.BAD_REQUEST, "AD_0012", "잘못된 날짜 범위입니다");
```

---

## 6. SecurityConfiguration 수정

```java
// SecurityConfiguration.java - adminApiFilterChain 수정
.authorizeHttpRequests(auth -> {
    // 인증 불필요
    auth.requestMatchers(HttpMethod.GET, "/api/admin/orgs").permitAll();
    auth.requestMatchers(HttpMethod.POST, "/api/admin/register").permitAll();
    auth.requestMatchers(HttpMethod.POST, "/api/admin/login").permitAll();

    // 인증 필요
    auth.requestMatchers("/api/admin/**").authenticated();
})

// securityFilterChain 수정 - User 비활성화 API 추가
// EumeUser API
auth.requestMatchers(HttpMethod.POST, "/api/users/me/deactivate").authenticated();
auth.requestMatchers(HttpMethod.POST, "/api/users/me/withdraw").authenticated();
```

---

## 7. 파일 생성 요약

### Controller (2개 수정)
| 파일명 | 설명 |
|--------|------|
| `EumeUserController.java` | deactivate, withdraw 메서드 추가 |
| `AdminController.java` | 상태변경, export, 보고서 메서드 추가 |

### Service (3개)
| 파일명 | 설명 |
|--------|------|
| `EumeUserService.java` | deactivateUser, withdrawUser 메서드 추가 |
| `AdminExportService.java` | 신규 생성 - Excel 내보내기 |
| `AdminReportService.java` | 신규 생성 - 보고서 통계 |

### DTO - Request (1개)
**위치:** `domain/dto/request/`

| 파일명 | 설명 |
|--------|------|
| `AdminUserStatusUpdateRequest.java` | 상태 변경 요청 |

### DTO - Response (8개)
**위치:** `domain/dto/response/`

| 파일명 | 설명 |
|--------|------|
| `UserDeactivateResponse.java` | 계정 비활성화 응답 |
| `UserWithdrawResponse.java` | 계정 탈퇴 응답 |
| `AdminUserStatusUpdateResponse.java` | 상태 변경 응답 |
| `AdminReportSummaryResponse.java` | 보고서 요약 응답 |
| `UserActivitySummary.java` | 이용자 활동 요약 |
| `ConversationSummary.java` | 대화 요약 |
| `EmotionSummary.java` | 감정 분석 요약 |
| `DailyStatResponse.java` | 일별 통계 |

### Entity 수정 (1개)
| 파일명 | 수정 내용 |
|--------|----------|
| `EumeUser.java` | deactivate(), activate(), withdraw(), isDeactivated() 메서드 추가 |

### ErrorCode 수정 (2개)
| 파일명 | 수정 내용 |
|--------|----------|
| `EumeUserErrorCode.java` | ALREADY_DEACTIVATED, DEACTIVATED_ACCOUNT 추가 |
| `AdminErrorCode.java` | INVALID_STATUS, EXPORT_FAILED, INVALID_DATE_RANGE 추가 |

### Repository 수정 (1개)
| 파일명 | 수정 내용 |
|--------|----------|
| `EumeUserRepository.java` | 통계 메서드 추가 |

---

## 8. 구현 순서 권장

### Phase 4: User 계정 비활성화 + Admin 상태 변경 (우선순위 높음)
1. EumeUser Entity 수정 (deactivate, activate, withdraw 메서드)
2. EumeUserErrorCode 에러 코드 추가
3. EumeUserService 메서드 추가
4. UserDeactivateResponse, UserWithdrawResponse DTO 생성
5. EumeUserController 메서드 추가
6. AdminErrorCode 에러 코드 추가
7. AdminUserStatusUpdateRequest, AdminUserStatusUpdateResponse DTO 생성
8. AdminService 상태 변경 메서드 추가
9. AdminController 상태 변경 메서드 추가
10. SecurityConfiguration 수정
11. 빌드 확인

### Phase 5: Admin Excel 내보내기
1. build.gradle에 Apache POI 의존성 추가
2. AdminExportService 생성
3. AdminSearchService에 findAllUsers 메서드 추가
4. AdminController export 메서드 추가
5. 빌드 확인

### Phase 6: Admin 보고서 (후순위)
1. Repository 통계 메서드 추가
2. AdminReportService 생성
3. 보고서 관련 DTO 생성
4. AdminController 보고서 메서드 추가
5. 빌드 확인

---

## 9. 의존성 추가 (build.gradle)

```gradle
dependencies {
    // ... 기존 의존성

    // Apache POI (Excel 파일 처리)
    implementation 'org.apache.poi:poi-ooxml:5.2.5'
}
```

---

## 10. 추가 고려 사항

### 계정 비활성화 시 처리
- JWT 쿠키 즉시 삭제
- 비활성화된 계정으로 로그인 시도 시 차단
- Google OAuth 연동은 유지 (재활성화 대비)

### Excel 내보내기 제한
- 최대 내보내기 행 수 제한 검토 (예: 10,000행)
- 대량 데이터 시 비동기 처리 고려

### 보고서 캐싱
- 동일 기간 반복 조회 시 캐싱 적용 검토
- 일별 통계는 전날까지만 캐싱 가능

### Admin 권한 분리 (추후)
- 현재: 모든 Admin이 동일 권한
- 추후: 기관별 Admin 권한 분리 검토
